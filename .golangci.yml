linters:
  enable:
    - errorlint
    - gocritic
    - gofmt
    - ineffassign
    - misspell
    - noctx
    - prealloc
    - sqlclosecheck
    - staticcheck
    - unconvert
    - unused
    - wastedassign

  disable:
    - bodyclose
    - dupl
    - errcheck
    - goconst
    - interfacer
    - unparam

issues:
  exclude:
    - "`?encodeFixed(32|64)Pb`? is unused"

# vim: set et shiftwidth=2:
#
